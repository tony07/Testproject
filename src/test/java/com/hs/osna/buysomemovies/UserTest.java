package com.hs.osna.buysomemovies;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

public class UserTest {

	/*
	 * @Test
	 * 
	 * @DisplayName("Check User Complete") public void checkUserComplete() { User
	 * user = new User(); user.setUsername("Hallo");
	 * Assertions.assertEquals("Hallo", user.getUsername());
	 * user.setPassword("123456789"); Assertions.assertEquals("123456789",
	 * user.getPassword()); user.setCredit(100); Assertions.assertEquals(100,
	 * user.getCredit()); boolean checkNull = user.equals(null);
	 * Assertions.assertFalse(checkNull); boolean identical = user.equals(user);
	 * Assertions.assertTrue(identical); boolean notSameUser = user.equals(new
	 * User("Test","12345678")); Assertions.assertFalse(notSameUser); }
	 */
	
}
